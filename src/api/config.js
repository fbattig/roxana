/**
 * API Configuration
 * 
 * This file centralizes API configuration to make it easier to change
 * the API URL in one place.
 */

// In development, the API is proxied through the Vite dev server
// In production, this would be replaced with the actual API URL
export const API_URL = '/api';
